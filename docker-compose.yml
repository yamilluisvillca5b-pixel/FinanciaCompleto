services:

  backend:
    build: ./backend-control-gastos
    container_name: backend-financia
    ports:
      - "3001:3001"

  frontend:
    build: ./frontend-control-gastos
    container_name: frontend-financia
    ports:
      - "5173:5173"
    depends_on:
      - backend